node_modules/
__pycache__/
.DS_Store
.env
.env.*

# Build artifacts
dist/

# Coverage artifacts
coverage/
cov-tmp/

# Vendored local binaries (platform-specific, not committed)
.bin/

# GSD artifacts. Unlike the other repos this blankets all of .claude/ on
# purpose: install.js deploys agents/, commands/, hooks/ and gsd-ng/ here when
# the installer is exercised, so everything under it is generated output.
/.claude/
/.planning/

# Default output of npm run evidence:clean-path; the phase copy is committed
# under .planning/ via an explicit --out-dir
/evidence/
